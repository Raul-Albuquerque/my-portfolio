import { Audiowide, Fira_Sans, Roboto } from 'next/font/google'

export const audiowide = Audiowide({
  weight: ['400'],
  subsets: ['latin'],
})

export const firaSans = Fira_Sans({
  weight: ['400', '700'],
  subsets: ['latin'],
})

export const roboto = Roboto({
  weight: ['400'],
  subsets: ['latin'],
})
